# 安全运营架构图

```mermaid
flowchart TB
    subgraph 日志源层["📡 日志源层"]
        EDR["🖥️ EDR终端\n青藤/360/火绒"]
        WAF["🛡️ WAF\nWeb应用防火墙"]
        FW["🔥 防火墙\nFlow/NetFlow"]
        AD["👤 AD域控\n账户/登录事件"]
        CLOUD["☁️ 云平台\n阿里云/华为云"]
        K8S["📦 容器集群\nK8s Audit"]
        NET["🌐 网络设备\n交换机/路由器"]
        DAM["🗄️ 数据库审计\nDAM"]
    end

    subgraph 采集层["🔄 采集层"]
        VECTOR["Vector\n日志采集Agent\n高性能/低占用"]
    end

    subgraph SIEM层["📊 SIEM 平台"]
        NORM["🔧 归一化引擎\n统一字段格式"]
        RULE["⚙️ 关联规则引擎\n复合告警生成"]
       分级["🚦 告警分级\nP0-P4"]
        检索["🔍 IOC快速检索\nIP/域名/Hash"]
        UEBA["📈 UEBA\n用户行为分析"]
    end

    subgraph SOAR层["🎭 SOAR 编排 Shuffle"]
        接收["📥 告警接收\nWebhook/API"]
        去重["🔁 去重聚合\n同源合并"]
        剧本["📋 剧本编排\n可视化编辑器"]
        审批["👆 人工审批节点\n钉钉/企微/邮件"]
        执行["⚡ 自动化执行\n封锁/隔离/禁用"]
    end

    subgraph 案件层["📁 案件管理 TheHive"]
        工单["📝 工单创建\n告警→工单"]
        分配["👥 调查分配\n按类型/负载"]
        流转["➡️ 状态流转\n调查→处置→闭环"]
        SLA["⏱️ SLA监控\n超时预警+升级"]
        笔记["📎 调查笔记\n附件/截图/日志"]
    end

    subgraph 情报层["🎯 威胁情报"]
        外部情报["🌍 外部情报\nSTIX/TAXII"]
        内部情报["🏠 内部IOC\n自产共享"]
    end

    subgraph 指标层["📉 运营指标"]
        MTTD["⏱️ MTTD\n威胁检测时间"]
        MTTR["⏱️ MTTR\n响应处置时间"]
        处置率["📊 自动化处置率"]
        闭环率["📊 工单闭环率"]
    end

    日志源层 --> 采集层
    采集层 --> SIEM层
    NORM --> RULE
    RULE --> 分级
    分级 --> 检索
    分级 --> 接收
    外部情报 --> RULE
    外部情报 --> 检索
    接收 --> 去重
    去重 --> 剧本
    剧本 --> 审批
    剧本 --> 执行
    执行 --> 工单
    执行 -.->|"封锁IP"| FW
    执行 -.->|"隔离主机"| EDR
    执行 -.->|"禁用账号"| AD
    工单 --> 分配
    分配 --> 流转
    流转 --> SLA
    流转 --> 笔记
    笔记 -.->|"误报反馈"| RULE
    工单 -.->|"闭环回写"| SIEM层
    内部情报 -.->|"IOC上报"| 外部情报
    流转 --> 指标层
    分级 --> 指标层

    style 日志源层 fill:#1a1a2e,stroke:#00d4ff,color:#fff
    style 采集层 fill:#16213e,stroke:#00d4ff,color:#fff
    style SIEM层 fill:#0f3460,stroke:#e94560,color:#fff
    style SOAR层 fill:#0f3460,stroke:#e94560,color:#fff
    style 案件层 fill:#533483,stroke:#e94560,color:#fff
    style 情报层 fill:#1a1a2e,stroke:#ffc107,color:#fff
    style 指标层 fill:#16213e,stroke:#00d4ff,color:#fff
```

---

## 数据流向说明

```
① 日志产生
   └─ 各安全设备/系统产生原始日志

② 采集归一化
   └─ Vector Agent 采集 → 统一字段格式（时间/IP/用户/行为/结果）

③ 关联分析
   └─ SIEM 规则引擎 → 多源日志关联 → 生成复合告警

④ 告警分级
   └─ P0（紧急）/ P1（高）/ P2（中）/ P3（低）/ P4（提示）

⑤ SOAR 研判
   ├─ 低置信度 → 标记误报 → 回写 SIEM 规则优化
   ├─ 中置信度 → 创建 TheHive 工单 → 分析师调查
   └─ 高置信度 → 自动处置（封锁/隔离/禁用）→ 人工审批后执行

⑥ 案件管理
   └─ 调查 → 处置 → 验证 → 闭环 → 运营指标统计

⑦ 情报驱动
   └─ 外部情报输入 + 内部 IOC 输出共享
```
