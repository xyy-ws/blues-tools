- 用户新增长期偏好：方案确认后可自主执行，无需二次审批；每个里程碑完成后主动推送简报。
- 用户要求暂停 EvoMap docker 任务跟进，已停用对应 watcher（job `33d9a28e-5475-465f-9ad8-f3397c919eaa`）。
- 用户当前关注 Telegram 语音转文字延迟，已发起“10条连续语音压测”请求；我已给出执行方式（1-3秒短语音按1~10序号发送，统计平均/P50/P90/最慢/最快/抖动）。
- 用户询问 verification-qa-guard 不可用原因；核对发现是“会话隔离/可见性差异”：`agents_list` 仅显示 `main`，但实际存在 label 为 `verification-qa-guard` 的子代理会话（`agent:main:subagent:7354...`）。
- 已向用户给出修复方向：自动流程优先按 session label 查找并复用现有验证会话，找不到再降级为 BLOCKED，避免误判“验证代理不存在”。
- 用户确认昵称为 `xyy`，并要求后续如此称呼；已同步更新 `USER.md`。
- 语音链路最终验收通过：Telegram 语音可识别并回复；A档延迟优化已应用（faster-whisper `base` + `beam-size=1`）。
- 仓库治理出现过“新任务误入 master/错分支”问题，已由用户确认后修复：
  - 新建 `feature/voice-telegram-discord` 承载语音任务。
  - `master` 回退到 `84caaed`。
  - `evomap-workbench` 回退到 `d7b141b`，并建立备份分支 `backup/evomap-workbench-before-cleanup`。
- 用户确认并执行“仓库级项目隔离”：已拆分并推送 4 个独立仓库：
  - `info-push`
  - `voice-chain`
  - `evomap-workbench`
  - `openclaw-workspace`
- 新建统一进度仓库 `task-progress`，作为后续任务进度唯一来源；已初始化 `tasks/` 与 `daily/` 并写入当前任务卡。
- `voice-chain` 已发布可安装成果：GitHub Release `v0.1.0`（含 `install.sh`、OpenClaw 配置与 env 示例）。
- `codex-auth-plugin` 已独立成仓并同步技能与发布文档；后续在该仓库单独维护发布节奏。
- 语音链路成果已发布到 EvoMap 并自动通过：
  - `bundle_id: bundle_a49a5bfbda065f68`
  - decision: `accept`（`skip_review_auto_promoted`）
- EvoMap Docker 任务仍存在平台侧状态不一致：`claim=task_full`、`complete=submitted`、但任务状态仍 `open`；证据已落盘到 `evomap-workbench/docs/incidents/2026-02-22-docker-task-state-inconsistency.md`。
- 已创建长期“验收子代理” `verification-qa-guard`（session: `agent:main:subagent:b533615e-5e89-4f57-8fed-ebfd8643eb2e`），并将其固定系统提示持久化到 `docs/agents/verification-qa-guard-prompt.md`。
- 用户确认后已修复 OpenClaw 插件配置错误（补回 `tools/openai-codex-auth-plugin/openclaw.plugin.json`），配置校验恢复正常。
- 子代理并发已正式写入网关配置：`agents.defaults.subagents.maxConcurrent = 2`（重载成功）。
- 用户确认“EvoMap 流程复验 SOP（5步）”可作为标准：fetch open task -> claim>=1 -> 执行产出 -> 子代理验收 -> submit/complete 回执。
- 实测复验仍被平台阻塞：fetch 可见 open tasks，但多轮 claim 全部 `task_full`；验收结论维持 `BLOCKED`（前置条件不满足，非实现失败）。
- 用户要求外部检索 `task_full` 解决方案（Tavily/GitHub）；结果未找到可靠官方修复指引，公开 issue 命中接近 0，当前仍以证据驱动容错流程处理。
- 账单接口核验：`/billing/earnings/{node_id}` 当前返回 `401 unauthorized`，在无登录态下无法直接读取准确 credit；节点公开信息可读（如 reputation=50, promoted=9）。
- 用户截图显示套餐页 `0 credits`，与先前口述 `600` 不一致；判断需区分“账户计费页余额/展示口径”与任务侧状态，`task_full` 更像任务池状态问题而非 API 限流。
- 2026-02-22 11:00+8 附近再次复验 question 池：fetch 到 10 个任务但 claim 全 `task_full`，暂无可接任务。
- 用户手动 Dispatch 某 bounty 后，代理侧仍无法在公开 fetch 视图看到 `claimed_by=node_2928238f2cbeef3d`；已记录新的可复现现象：前端已操作 Dispatch，但后端公开接口仍呈现 open/不可 claim（需 task_id 私有态核对）。
- 用户提供 bounty 链接：`https://evomap.ai/bounty/cm50dd50d2a1c13d1551ff8b5`；该页面直连返回 Next.js HTML（非直出任务 JSON），进一步佐证需要登录态/私有接口字段（task_id）才能稳定追踪派发任务。
- 用户要求排查“OpenClaw 接入 Discord 后不主动发信息”问题；已确认 Discord 频道权限与配置（allow=true / requireMention=false / groupPolicy=open）基本正确，且 `message.send` 主动直发可成功。当前根因更偏向 cron `delivery.mode=announce` 链路不稳定（存在 run 成功但频道无感知）；后续策略建议改为“cron 负责触发 + `message.send` 直发兜底/主路径”。
- 用户同意将 EvoMap 自动轮询改为 Discord 直发主路径：已将 job `9b0a1445-1c2a-41a7-a6ef-32a3c90a883d` 的 `delivery.mode` 调整为 `none`，并要求每轮通过 `message.send` 发摘要到频道 `1474265559716593707`。
- 为降低风控与 403/5xx，用户同意将轮询频率从 1 分钟降为 5 分钟，后又降为 1 小时（`everyMs=3600000`）；并固化指数退避（30s→60s→120s）。
- 用户要求 fetch 提升到 30 条；已更新自动任务提示为 `limit=30`，并补充 `source_status` 字段（`fetch_http` + `parse_ok`）用于区分“真实空池”与“链路异常”。
- 已确认 EvoMap 返回存在波动：同周期可能出现 `fetched=10`、`fetched=0`、`403`、`timeout`；手动重试可恢复 `HTTP 200` 且常见仅返回 10 任务（即使请求 limit=30）。
- 已按用户要求复核历史任务：`/a2a/task/my` 显示总 20，`pending=19`、`rejected=1`；该 rejected 对应任务状态为 `completed`，当前策略为“rejected 也处理：open 则重做重提，completed 则归档无需重提并回执”。
- 用户确认将 verification-qa-guard 升级为“独立可复用验证代理”；已创建并连通性确认（回复 READY）会话 `agent:main:subagent:8356a7b4-b307-44c2-a6e0-c9922b3f023f`，并已写入 EvoMap 自动任务规则：需验证任务必须调用该会话，非 PASS 禁止提交。
- 用户询问“独立代理与会话关系/是否新开会话”；已明确默认复用固定验证会话，仅在不可用时临时兜底新开并在回执标注。
- 用户多次要求“再试一下”后，手动复验结果为：fetch 可用时可拿到 10 个 open 任务，但 claim 仍常见 `task_full`；当前结论仍是“可见但难以成功认领”。

## Backfill (2026-02-27)
- 可访问会话中未检索到当天有效记录（仅据本次可访问范围）。
- 任务：无新增可确认任务。
- 风险：历史上下文可能缺失，需在后续可访问性恢复后补证。
- 待办：无。
